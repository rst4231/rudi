name: Inspect Turbulence schedule source

"on": push

permissions:
  contents: read

jobs:
  inspect:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 24
      - run: npm install
      - name: Parse server-side Mirage schedules
        run: |
          node <<'NODE'
          const { loadMiragePremieres } = require('./api/cinema-premieres.cjs');
          const urls = [
            'https://app.mirage.ru/msk/schedule/cinema/24/',
            'https://www.mirage.ru/arh/schedule/03.09.2026/'
          ];
          (async () => {
            for (const url of urls) {
              try {
                const rows = await loadMiragePremieres('2026-09-03', { name: 'Мираж Синема', url }, {
                  fetchImpl: globalThis.fetch,
                  attempts: 2,
                  timeoutMs: 15000,
                });
                console.log(JSON.stringify({ url, count: rows.length, titles: rows.map((x) => x.title), posterUrls: rows.map((x) => x.posterUrl), sourceUrls: rows.map((x) => x.sourceUrl) }, null, 2));
              } catch (error) {
                console.log(JSON.stringify({ url, error: String(error?.message || error) }));
              }
            }
          })().catch((error) => { console.error(error); process.exit(1); });
          NODE
