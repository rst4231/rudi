const test=require('node:test');const assert=require('node:assert/strict');const {getNativeSectionRunner,runNativeSection}=require('../api/section-runners.cjs');
test('only native sections expose direct runners',()=>{assert.equal(typeof getNativeSectionRunner('cinema',{cinema:async()=>({})}),'function');assert.equal(typeof getNativeSectionRunner('labor',{labor:async()=>({})}),'function');assert.equal(typeof getNativeSectionRunner('weekend',{weekend:async()=>({})}),'function');assert.equal(getNativeSectionRunner('facts',{}),null);});
test('failed native runner retries at most twice then alerts',async()=>{let calls=0,alerts=0;await assert.rejects(()=>runNativeSection('cinema',{date:'2026-08-30',runner:async()=>{calls++;throw new Error('boom')},getRecord:async()=>null,markPending:async()=>{},markFailed:async()=>{},alert:async()=>{alerts++},maxAttempts:2}),/boom/);assert.equal(calls,2);assert.equal(alerts,1);});
